name: Azure Coontainer Apps CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job

    env:
      ACR_NAME: anhnguyenkimacr
      CONTAINER_APP_NAME: local-api
      RESOURCE_GROUP: local-rg
      IMAGE_NAME: samples/hello-world
      LOCATION: westus2

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # Login to Azure with SP credentials
      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Build and push image to ACR
        run: |
          IMAGE_TAG=${{ github.sha }}
          az acr build \
            --registry $ACR_NAME \
            --image $IMAGE_NAME:$IMAGE_TAG \
            .

      - name: Deploy to Container App
        run: |
          IMAGE_TAG=${{ github.sha }}
          az containerapp update \
            --name $CONTAINER_APP_NAME \
            --resource-group $RESOURCE_GROUP \
            --image $ACR_NAME.azurecr.io/$IMAGE_NAME:$IMAGE_TAG
      # - name: Get Container Registry
      #   id: acr-details
      #   run: |
      #     RESOURCE_GROUP=rg-terraform-backend
      #     ACR_NAME=anhnguyenkimacr
      #     ACR_USERNAME=$(az acr credential show --name $ACR_NAME --resource-group $RESOURCE_GROUP --query username --output tsv)
      #     ACR_PASSWORD=$(az acr credential show --name $ACR_NAME --resource-group $RESOURCE_GROUP --query "passwords[0].value" --output tsv)

      #     echo "ACR_LOGIN_SERVER=$ACR_LOGIN_SERVER" >> $GITHUB_OUTPUT
      #     echo "ACR_USERNAME=$ACR_USERNAME" >> $GITHUB_OUTPUT
      #     echo "ACR_PASSWORD=$ACR_PASSWORD" >> $GITHUB_OUTPUT

      # - name: Build and push container image to registry
      #   uses: azure/container-apps-deploy-action@v2
      #   with:
      #     appSourcePath: ${{ github.workspace }}
      #     registryUrl: anhnguyenkimacr.azurecr.io
      #     registryUsername: ${{ steps.acr-details.outputs.ACR_USERNAME }}
      #     registryPassword: ${{ steps.acr-details.outputs.ACR_PASSWORD }}
      #     containerAppName: local-api
      #     resourceGroup: local-rg
      #     imageToBuild: anhnguyenkimacr.azurecr.io/samples/hello-world:${{ github.sha }}
