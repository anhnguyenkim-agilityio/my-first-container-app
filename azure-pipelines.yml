trigger:
  branches:
    include:
      - develop
      - staging
      - main

pr:
  branches:
    include:
      - "*"

variables:
  - name: ACR_NAME
    value: "f2kwkhdb8vhvacr"
  - name: IMAGE_NAME
    value: "samples/hello-world"

  - name: GITHUB_SERVICE_CONNECTION
    value: "github.com_anhnguyenkim-agilityio"

  - name: AZURE_SUBSCRIPTION_SERVICE_CONNECTION
    value: "AzureConnection"

  - name: containerApps
    value: "dev-app"

pool:
  name: Default

resources:
  repositories:
  - repository: self
    type: github
    name: $(GITHUB_SERVICE_CONNECTION)/my-first-container-app

stages:
# 1. Build Stage
- stage: Build
  displayName: Build & Push Image
  jobs:
  - job: Build
    steps:
    # Set IMAGE_TAG and RG_NAME
    - bash: |
        echo "Branch name: $(Build.SourceBranchName)"
        # Detect environment from branch
        if [ "$(Build.SourceBranchName)" = "main" ]; then
          ENV_NAME="dev"
        elif [ "$(Build.SourceBranchName)" = "staging" ]; then
          ENV_NAME="staging"
        else
          ENV_NAME="dev"
        fi

        SHORT_SHA=$(echo $(Build.SourceVersion) | cut -c1-7)
        IMAGE_TAG="${ENV_NAME}-$SHORT_SHA"
        echo "Image tag: $IMAGE_TAG"

        echo "##vso[task.setvariable variable=imageTag;isOutput=true]$IMAGE_TAG"
        echo "##vso[task.setvariable variable=envName;isOutput=true]$ENV_NAME"
      name: SetEnvs
      displayName: "Set image tag"

    # Login to ACR
    - task: AzureCLI@2
      displayName: "Login to ACR"
      inputs:
        azureSubscription: "$(AZURE_SUBSCRIPTION_SERVICE_CONNECTION)"
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          az acr login --name $(ACR_NAME)

    - bash: |
        echo "Building image: $(ACR_NAME).azurecr.io/$(IMAGE_NAME):$(SetEnvs.imageTag)"
        docker build -t $(ACR_NAME).azurecr.io/$(IMAGE_NAME):$(SetEnvs.imageTag) .
        docker push $(ACR_NAME).azurecr.io/$(IMAGE_NAME):$(SetEnvs.imageTag)
      displayName: "Build & Push Docker Image"

# 2. Test Stage
- stage: Test
  displayName: Run Tests
  dependsOn: Build
  condition: or(eq(variables['Build.Reason'], 'PullRequest'), contains(['main','develop','staging'], variables['Build.SourceBranchName']))
  jobs:
  - job: RunTests
    steps:
      - bash: |
          echo "Running tests..."
        displayName: "Run Unit/Integration Tests"

# 3. Migrate Stage
- stage: Migrate
  displayName: Run Django Migrations Job
  dependsOn:
    - Test
    - Build
  condition: and(succeeded(), or(eq(variables['Build.SourceBranchName'], 'develop'), eq(variables['Build.SourceBranchName'], 'main')))
  jobs:
    - job: RunMigrationJob
      variables:
        # Reference output from Build job
        ENV_NAME: $[ stageDependencies.Build.Build.outputs['SetEnvs.envName'] ]
      steps:
        - task: AzureCLI@2
          displayName: "Run Django Migration Job"
          inputs:
            azureSubscription: "$(AZURE_SUBSCRIPTION_SERVICE_CONNECTION)"
            scriptType: bash
            scriptLocation: inlineScript
            inlineScript: |
              echo "Starting migration job..."
              az containerapp job start \
                --name django-migrate-job \
                --resource-group $ENV_NAME-rg

              echo "Waiting for job to finish..."

              # Fetch job execution status until 'Completed'
              JOB_EXEC_ID=$(az containerapp job execution list \
                --name django-migrate-job \
                --resource-group $ENV_NAME-rg \
                --query "[0].name" -o tsv)

              while true; do
                STATUS=$(az containerapp job execution show \
                  --name django-migrate-job \
                  --resource-group $ENV_NAME-rg \
                  --job-execution-name $JOB_EXEC_ID \
                  --query "properties.status" -o tsv)

                echo "Job status: $STATUS"

                if [ "$STATUS" = "Succeeded" ]; then
                  echo "Migration job succeeded."
                  break
                fi

                if [ "$STATUS" = "Failed" ]; then
                  echo "Migration job FAILED."
                  exit 1
                fi

                sleep 5
              done

# 4. Deploy Stage
- stage: Deploy
  displayName: "Deploy Container Apps"
  dependsOn:
    - Build
    - Migrate
  variables:
    # Reference output from Build job
    IMAGE_TAG: $[ stageDependencies.Build.Build.outputs['SetEnvs.imageTag'] ]
    ENV_NAME: $[ stageDependencies.Build.Build.outputs['SetEnvs.envName'] ]
  condition: and(succeeded(), or(eq(variables['Build.SourceBranchName'], 'main'), eq(variables['Build.SourceBranchName'], 'develop')))
  jobs:
    - deployment: DeployToEnv
      displayName: Deploy Container App
      environment: 'dev'

      strategy:
        runOnce:
          deploy:
            steps:
            - task: AzureCLI@2
              displayName: "Deploy all apps"
              inputs:
                azureSubscription: "$(AZURE_SUBSCRIPTION_SERVICE_CONNECTION)"
                scriptType: bash
                scriptLocation: inlineScript
                inlineScript: |
                  echo "Deploying image: $(IMAGE_NAME):$IMAGE_TAG"

                  az containerapp update \
                    --name $ENV_NAME-app \
                    --resource-group $ENV_NAME-rg \
                    --container-name api \
                    --image "$(ACR_NAME).azurecr.io/$(IMAGE_NAME):$IMAGE_TAG"

                  az containerapp update \
                    --name $ENV_NAME-app \
                    --resource-group $ENV_NAME-rg \
                    --container-name worker \
                    --image "$(ACR_NAME).azurecr.io/$(IMAGE_NAME):$IMAGE_TAG"

                  az containerapp update \
                    --name $ENV_NAME-app \
                    --resource-group $ENV_NAME-rg \
                    --container-name beat \
                    --image "$(ACR_NAME).azurecr.io/$(IMAGE_NAME):$IMAGE_TAG"
